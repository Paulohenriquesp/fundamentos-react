

// eslint-disable-next-line no-unused-vars
const Container = ({children, myValue}) => {
  return (
    <div>
        <h2>Testando um dois três</h2>
        {children}
        <p>o valor é {myValue} </p>
    </div>
    
  )
}

export default Container